/*26. What is the difference between #include <iostream.h> and #include <iostream>

In C ++ library files .h are old versions, many compilers no longer support. Library .h is not defined in namespace std.

Library files do NOT have .h is the new version, with the new version all standard libraries are defined in namespace std.*/
#include <iostream>// #include <iostream.h>
using namespace std;