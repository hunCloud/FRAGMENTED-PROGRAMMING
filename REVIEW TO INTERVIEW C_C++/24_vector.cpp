/*24. What does the concept of Vector mean?

Vector is "sequence containers" in STL, with linear sequential elements, we can access each element of the vector through the index of that element.

We can add, edit, delete elements in the vector easily and flexibly.*/